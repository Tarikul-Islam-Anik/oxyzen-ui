import { Steps } from "nextra/components";
import { FileTree } from "nextra/components";

# Getting Started

Start building your project in minutes.

<Steps>
### Install the necessary dependencies

Install `class-variance-authority`, `clsx`, `tailwind-merge` utility packages by running:

```sh copy npm2yarn
npm install class-variance-authority clsx tailwind-merge
```

### `@radix-ui` primitives

Now install necessary `@radix-ui` packages by running:

```sh copy npm2yarn 
npm install @radix-ui/react-slot
```

### Get the necessary files

Now download the **components**, **lib** and **hooks** folders from the [GitHub repository](https://github.com/Tarikul-Islam-Anik/oxyzen-ui/) and paste them into your project root directory.

{" "}

<FileTree>
  <FileTree.Folder name="components" defaultOpen>
    <FileTree.Folder name="layout">
      <FileTree.File name="box.tsx" />
      <FileTree.File name="container.tsx" />
      <FileTree.File name="flex.tsx" />
      <FileTree.File name="grid.tsx" />
      <FileTree.File name="section.tsx" />
      <FileTree.File name="variants.ts" />
    </FileTree.Folder>
    <FileTree.Folder name="typography">
      <FileTree.File name="blockquote.tsx" />
      <FileTree.File name="em.tsx" />
      <FileTree.File name="heading.tsx" />
      <FileTree.File name="strong.tsx" />
      <FileTree.File name="text.tsx" />
      <FileTree.File name="variants.ts" />
    </FileTree.Folder>
  </FileTree.Folder>
  <FileTree.Folder name="lib">
    <FileTree.File name="utils.ts" />
  </FileTree.Folder>
</FileTree>

</Steps>
